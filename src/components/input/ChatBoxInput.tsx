import React from "react";

const ChatBoxInput = () => {
    return (
        <div>
            <input type="text" placeholder="This is where user inputs their texts and links to get translations" />
        </div>
    );
}

export default ChatBoxInput;